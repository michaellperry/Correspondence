<Window x:Class="Reversi.Client.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Reversi.Client.View"
    Title="Correspondence Reversi" Height="555" Width="604">
    
    <DockPanel>
        <Border
            DockPanel.Dock="Top">
            <local:LogonView DataContext="{Binding Logon}" />
        </Border>
        <Border
            DockPanel.Dock="Bottom">
            <TextBlock
                Text="{Binding LastError}"
                TextTrimming="CharacterEllipsis" />

        </Border>
        <Border
            DockPanel.Dock="Left"
            Width="160">
            <local:GameListView />
        </Border>
        <Border Background="Green">
            <local:GameBoardView />
        </Border>
    </DockPanel>
</Window>
